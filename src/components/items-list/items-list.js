import "./items-list.scss";
const ItemsList = () => {
  const ItemListElement = document.createElement("div");
  ItemListElement.className = "items-list";
  // ItemListElement.id = "items-list";

  return ItemListElement;
};

export default ItemsList;
